import type { ArchiveEvent } from "@/types/archive.type";
const colors:Record<string,string>={green:"text-emerald-300",yellow:"text-amber-300",sc:"text-amber-200",vsc:"text-yellow-200",red:"text-rose-400",chequered:"text-white",penalty:"text-rose-300","track-limits":"text-orange-300"};
export default function EventsLog({events}:{events:ArchiveEvent[]}){return <div className="flex flex-col gap-1">{events.map((event,index)=><div key={`${event.utc}.${event.kind}.${index}`} className="data-chip grid grid-cols-[7rem_7rem_1fr] gap-3 rounded p-2 font-mono text-xs"><time className="text-zinc-500">{event.utc.slice(11,19)}</time><span className={colors[event.kind]??"text-cyan-300"}>{event.kind.toUpperCase()}</span><span>{event.driverNr&&`#${event.driverNr} `}{event.message}</span></div>)}</div>}
